# Secrets — never commit these
.streamlit/secrets.toml

# Runtime shared state (cross-session persistence — never commit)
data/*.json

# Python
__pycache__/
*.py[cod]
*.pyo
.env

# Streamlit cache
.streamlit/cache/

# OS
.DS_Store
Thumbs.db

# Temp / junk files (keep requirements.txt!)
btn.textContent
pd.DataFrame
str
this.textContent
C:UsersmelmoDesktopskill_map_tmp.txt
